import 'package:angular2/platform/browser.dart';

import 'package:github.daveshuckerow.chat.web/app_component.dart';

main() {
  print('starting the app up.');
  bootstrap(AppComponent, providers: bindings);
}
